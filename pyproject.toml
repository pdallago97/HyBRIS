[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hybris"                   
version = "0.1.1"
description = "Optical, radar, and hybrid indices to detect farming practices"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"                    
license-files = ["LICENSE"]
authors = [{ name = "Paolo Dal Lago", email = "paolo.dallago@wur.nl" }]
keywords = ["remote sensing", "Sentinel-1", "Sentinel-2", "agriculture", "tillage"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: GIS",
]
dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "scipy>=1.10",
    "matplotlib>=3.7",
]

[project.optional-dependencies]
gee = ["earthengine-api>=1.0"]
examples = ["geopandas>=1.0", "contextily>=1.6", "xyzservices"]

[tool.hatch.build.targets.sdist]
include = ["/src", "/tests", "/README.md", "/LICENSE", "/pyproject.toml"]

[project.urls]
Homepage = "https://github.com/pdallago97/HyBRIS"
Paper = "https://doi.org/10.1016/j.rse.2026.115553"
Issues = "https://github.com/pdallago97/HyBRIS/issues"